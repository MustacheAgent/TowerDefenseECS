using PeartreeGames.BlockyWorldEditor;
using UnityEngine;

namespace Test
{
    public class BlockyTest : MonoBehaviour
    {
        public BlockyObjectMap map;
        
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
